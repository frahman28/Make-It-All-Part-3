<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="../css/formatClean.css" />

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="../css/employee_styles.css" />
    <script src="https://cdn.jsdelivr.net/npm/@tarekraafat/autocomplete.js@10.2.7/dist/autoComplete.min.js"></script>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/@tarekraafat/autocomplete.js@10.2.7/dist/css/autoComplete.min.css"
    />
    <link rel="stylesheet" href="../css/createEmployee.css" />
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script src="../js/createEmployee.js"></script>
    <title>Create employee</title>
  </head>
  <body style="min-height: 100vh; position: relative; overflow-x: hidden">
    <div class="text-center image-div">
      <img
        class="text-center logo"
        src="../images/logo.png"
        alt="make-it-all"
      />
    </div>
    <nav class="navbar navbar-expand-lg bg-dark navbar-dark">
      <div class="container-fluid">
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#main-links"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="main-links">
          <ul class="navbar-nav">
            <% if (role == 'specialist' || role == 'employee') { %>
            <li class="nav-item px-4">
              <a href="../myProblems" class="nav-link">Home</a>
            </li>
            <% } else { %>
            <li class="nav-item px-4">
              <a href="../dashboard" class="nav-link">Home</a>
            </li>
            <% } %> <% if (role == 'specialist' || role == 'employee') { %>
            <li class="nav-item px-4">
              <a href="../submitProblem" class="nav-link">Submit a Problem</a>
            </li>
            <% } %>
            <li class="nav-item px-4">
              <a href="../allProblems" class="nav-link">All Problems</a>
            </li>
            <% if (role == 'admin') { %>
            <li class="nav-item px-4">
              <a href="../allEmployees" class="nav-link">All Employees</a>
            </li>
            <li class="nav-item px-4">
              <a href="../create-employee" class="nav-link">New Employee</a>
            </li>
            <% } %> <% if (role == 'admin' || role == 'specialist') { %>
            <li class="nav-item px-4">
              <a href="/problem-type/manage-problem-types" class="nav-link"
                >Edit problem types</a
              >
            </li>
            <% } %> <% if (role == 'admin' || role == 'specialist' || role ==
            'employee') { %>
            <li class="nav-item px-4">
              <a href="../viewEquipment" class="nav-link">Equipment</a>
            </li>
            <% } %>
          </ul>
          <ul class="navbar-nav ms-auto">
            <li class="nav-item dropdown px-4">
              <a
                class="nav-link dropdown-toggle"
                href="#"
                id="navbarDropdownMenuLink"
                role="button"
                data-bs-toggle="dropdown"
                aria-expanded="false"
              >
                <!-- Render user's username -->
                <%= userName %>
              </a>
              <ul
                class="dropdown-menu bg-dark dropdown-menu-dark"
                aria-labelledby="navbarDropdownMenuLink"
              >
                <li>
                  <a class="dropdown-item" href="../settings">Settings</a>
                </li>
                <li><a class="dropdown-item" href="../logout">Logout</a></li>
              </ul>
            </li>
          </ul>
        </div>
      </div>
    </nav>
    <br /><br />

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
      crossorigin="anonymous"
    ></script>

    <div class="container">
      <section class="container-fluid">
        <form action="" id="problemSubmissionForm">
          <label for="employee-id">Employee ID</label>
          <input
            type="number"
            name="employee-id"
            id="employee-id-input"
            min="100000"
            max="999999"
            required
          />
          <small class="error" id="employee-id-error"
            >Employee ID already exists</small
          >
          <br />
          <label for="employee-name">Employee name</label>
          <input
            type="text"
            name="employee-name"
            id="employee-name-input"
            required
          />
          <label for="role-id">Role</label>
          <select name="role-id" id="role-id-input" required>
            <% for (var j = 0; j < companyRoles.length; j++ ) {%>
            <!-- Loop through types to display and automatically display selected rows type -->
            <option value="<%= companyRoles[j]['role_id'] %>">
              <%= companyRoles[j]["role"] %>
            </option>
            <% } %>
          </select>
          <label for="title">Job Title</label>
          <select name="title" id="title-input" required>
            <% for (var j = 0; j < jobTitles.length; j++ ) {%>
            <!-- Loop through types to display and automatically display selected rows type -->
            <option value="<%= jobTitles[j]['title_id'] %>">
              <%= jobTitles[j]["title"] %>
            </option>
            <% } %>
          </select>
          <label for="department">Department</label>
          <select name="department" id="department-input" required>
            <% for (var j = 0; j < departments.length; j++ ) {%>
            <!-- Loop through types to display and automatically display selected rows type -->
            <option value="<%= departments[j]['department_id'] %>">
              <%= departments[j]["name"] %>
            </option>
            <% } %>
          </select>
          <label for="ext-num">Extension Number</label>
          <input
            type="number"
            name="ext-num"
            id="ext-num-input"
            min="100"
            max="999"
            required
          />
          <br />
          <br />
          <select name="external" id="external-input" required>
            <option value="0">Internal employee</option>
            <option value="1">External employee</option>
          </select>
          <br />
          <br />
          <hr />
          <label for="login-username">Login username </label>
          <input
            type="text"
            name="login-username"
            id="login-username-input"
            required
          />
          <small class="error" id="login-username-error"
            >Username already in use</small
          >
          <br />
          <label for="login-password">Login password</label>
          <input
            type="password"
            name="login-username"
            id="login-password-input"
            required
          />
          <br />
          <br />
          <button type="submit" class="mb-4 mt-2 btn-lg w-100">
            Create Employee
          </button>
        </form>
        <br />
        <br />
        <br />
      </section>
    </div>
  </body>
</html>
